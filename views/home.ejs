<link rel="stylesheet" href="/styles/homepage.css">
<%- include('./partials/_homeHeader'); %>

<!-- <div class="mb-5 p-5">
  <% for( let project of projects ) { %>
  <div class="card mb-2 w-50 m-auto">
    <div class="card-header">
      <strong> Name of the project : </strong><%= project.name %>
    </div>
    <div class="card-body">
      <h6 class="card-subtitle mb-2 text-muted">
        <strong> Author : </strong> <%= project.author %>
      </h6>
      <p class="card-text">
        <strong> Description : </strong><%= project.description %>
      </p>

      <div class="m-auto text-end">
        <a href="/project/<%=project._id%>" class="btn btn-primary m-auto"
          >Open project</a
        >
      </div>
    </div>
  </div>
  <% } %>
</div> -->


<div class="container">


  <% for( let project of projects ) { %>
    <!-- <div class="card mb-2 w-50 m-auto">
      <div class="card-header">
        <strong> Name of the project : </strong><%= project.name %>
      </div>
      <div class="card-body">
        <h6 class="card-subtitle mb-2 text-muted">
          <strong> Author : </strong> <%= project.author %>
        </h6>
        <p class="card-text">
          <strong> Description : </strong><%= project.description %>
        </p>
  
        <div class="m-auto text-end">
          <a href="/project/<%=project._id%>" class="btn btn-primary m-auto"
            >Open project</a
          >
        </div>
      </div>
    </div> -->


    <div class="row projectBox" onclick="location.href='/project/<%=project._id%>'">
      <div><strong> Name of the project : </strong><%= project.name %></div>
      <div><strong> Description : </strong><%= project.description %></div>
      <div><strong> Author : </strong> <%= project.author %></div>
      <div><strong> Time : </strong> <%= new Date(project.createdAt).toLocaleString();  %></div>
    </div>
    <% } %>
</div>